<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Splash screen theme -->
    <style name="Theme.SplitPay.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#2B348D</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.SplitPay</item>
    </style>

    <!-- Main app theme -->
    <style name="Theme.SplitPay" parent="android:Theme.Material.Light.NoActionBar" />

</resources>
